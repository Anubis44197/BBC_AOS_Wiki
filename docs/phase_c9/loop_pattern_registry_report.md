# Loop Pattern Registry Report

Default operational loop patterns are stored in `src/bbc_aos/operations/patterns.yaml`
and exposed through `bbc loop patterns`, `bbc loop describe`, and `bbc loop start`.
