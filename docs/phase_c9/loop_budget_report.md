# Loop Budget Report

The loop budget engine persists hard limits and counters in `.bbc/loop/budget.json`.
Default `max_iterations` remains 3. The CLI exposes `bbc loop budget`,
`bbc loop usage`, and `bbc loop cost`.
